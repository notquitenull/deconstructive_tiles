-- general variables for my sanity
local base = "__deconstructive_tiles__/" 
base_name = "deconstructive-tile-"
folder = {
	base = base,
	graphics = base .. "graphics/",
	prototypes = base .. "prototypes/"
}

tile_variants = {
	basic = "base",
	cautious = "cautious",
	ghost_only = "ghost-only",
}

cooldown = 60 * 5
